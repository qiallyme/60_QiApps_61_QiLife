# QiLife Data

Canonical local-first data layout:

```text
data/
  db/
    qilife.sqlite
  files/
  exports/
  backups/
```

The repository should treat `docs/` as canonical build knowledge and `data/` as runtime state.
