# Retired QiLife Prototype

This directory contains non-runnable historical source retained temporarily for reference.

It is not an application root, has no package manifest, has no runtime entrypoint, and must not be configured in Cloudflare.

The only canonical and deployable QiLife application is:

```text
qilife.2/
```

New implementation work belongs in `qilife.2`. Remaining historical source in this directory can be removed after the canonical app has been validated in production.
