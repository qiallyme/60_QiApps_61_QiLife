// QiLife uses vanilla CSS — no PostCSS plugins needed.
// This file exists to prevent Vite from walking up to C:\QiLabs\postcss.config.js
// (which requires @tailwindcss/postcss for a different project).
export default {
  plugins: {},
};
